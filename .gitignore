*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
# NOTE: debug.keystore is intentionally NOT ignored — it must stay committed
# so every CI build signs with the SAME fingerprint. Regenerating it breaks
# Google/Phone Sign-In because Firebase/Google Cloud only trust one SHA-1.
